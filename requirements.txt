# Downpour v29 Titanium - Dependencies
# Install: pip install -r requirements.txt --break-system-packages
#
# NOTE: Python 3.15.0a6 (alpha) — no pre-built wheels for C extension packages.
# All packages below are guarded by try/except ImportError; the app runs
# without them. Install with pip install <package> once cp315 wheels exist.

# Core dependencies (native .py or pure-Python, always installable)
psutil>=5.9.0
nvidia-ml-py>=12.0.0
cryptography>=41.0.0
requests>=2.31.0
dnspython>=2.4.0
netifaces>=0.11.0
scapy>=2.5.0
wmi>=1.5.1
pywin32>=306
colorama>=0.4.6
watchdog>=3.0.0

# Optional — system tray icon (requires C compiler on Python 3.15 alpha)
pystray>=0.19.0

# Optional — image processing & charts (requires C compiler on Python 3.15 alpha)
# pillow>=10.0.0
# matplotlib>=3.7.0

# ML/AI dependencies (optional - graceful fallback if missing)
numpy>=1.24.0
# scikit-learn>=1.3.0     # requires C compiler on Python 3.15 alpha

# Pattern matching (optional - graceful fallback if missing)
# yara-python>=4.3.0      # requires C compiler on Python 3.15 alpha
