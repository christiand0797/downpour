# Downpour v29 Titanium - Dependencies
# Install: pip install -r requirements.txt
#
# Tested with Python 3.11, 3.12, 3.13+ (3.12 recommended).
# All packages below are guarded by try/except ImportError in the app —
# it still runs if any optional one is missing, with graceful feature
# degradation. Recommended install command (from LAUNCH_DOWNPOUR.bat):
#
#   pip install psutil requests cryptography watchdog nvidia-ml-py colorama \
#     wmi pywin32 scikit-learn yara-python pillow dnspython joblib \
#     tqdm pyperclip reportlab pystray matplotlib GPUtil setuptools

# ── Core dependencies (used by every run, keep always present) ──────────────
psutil>=6.1.0            # system metrics (CPU/RAM/disk/net/processes) - latest
nvidia-ml-py>=13.590.0   # GPU telemetry via NVML (falls back to GPUtil / nvidia-smi)
cryptography>=43.0.0     # quarantine encryption, hashing (latest)
requests>=2.32.0         # threat feeds, OSINT lookups, intel updates
urllib3>=2.2.0,<3.0.0    # HTTP library with compatible version for requests
charset-normalizer>=3.3.0,<4.0.0  # Character encoding compatibility for requests
dnspython>=2.6.0         # DNS advanced tools
scapy>=2.6.0             # network scanning / packet analysis
wmi>=1.5.1               # CPU temp, AV status, WMI sensors
pywin32>=310             # win32 registry, services, process APIs
colorama>=0.4.6          # terminal colors
watchdog>=4.0.0          # file integrity monitoring (FIM)
numpy>=2.0.0             # ML / behavioral analysis backend

# ── Optional (feature toggles, graceful fallback when missing) ──────────────
# YARA-X: VirusTotal's Rust YARA rewrite — 5-10x faster scanning of the
# yara_rules/ rulesets (falls back to yara-python when absent)
yara-x>=1.23.0
# System tray icon + restore window
pystray>=0.19.5
# Floating widgets, tray icon rendering, image analysis
pillow>=10.4.0
# Real PDF export for security / compliance reports
reportlab>=4.2.0
# Charting / matplotlib-backed analytics (uses Agg backend, headless-safe)
matplotlib>=3.9.0
# ML behavior scoring (isolated forest / random forest) — falls back to
# heuristic scoring when absent
scikit-learn>=1.5.0
# Pattern matching signatures (fallback: regex engine)
yara-python>=4.5.0
# GPU fallback path when nvidia-ml-py is unavailable
GPUtil>=1.4.1
# Misc helpers used by cleanup / clipboard / progress paths
joblib>=1.4.0
tqdm>=4.66.0
pyperclip>=1.9.0

# ── Additional dependencies ──────────────
netifaces>=0.11.0        # network interface enumeration
scipy>=1.13.0            # signal/entropy calculations
pefile>=2024.8.26        # PE header analysis
pytest>=8.2.0            # test runner
# Additional modern dependencies
pydantic>=2.7.0          # data validation for configs
pyyaml>=6.0.1            # YAML config support
tomli>=2.0.1             # TOML config support (stdlib in 3.11+)
aiohttp>=3.9.0           # async HTTP for threat feeds
aiodns>=3.1.0            # async DNS resolution
click>=8.1.0             # CLI utilities
rich>=13.7.0             # rich terminal output
packaging>=24.0          # version parsing
importlib-metadata>=8.0  # metadata access